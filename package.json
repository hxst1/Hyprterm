{
  "name": "hyprterm",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter hyprterm-app dev",
    "build": "pnpm --filter hyprterm-app build",
    "start": "pnpm --filter hyprterm-server start",
    "setpass": "pnpm --filter hyprterm-server setpass",
    "test": "pnpm -r test",
    "setup": "bash setup.sh"
  }
}
